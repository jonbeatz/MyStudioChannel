I need you to redesign the TaskBoardAI Kanban interface into a premium, cinematic, production-grade dashboard that matches the MyStudioChannel brand aesthetic and integrates seamlessly with my existing local AI stack. The project is a vanilla HTML/CSS/JS Express app, so the redesign must preserve that architecture.

## 1. The Brand & Design Language

### Brand Profile: MyStudioChannel
- **Primary Accent:** Studio Gold (#F5B841)
- **Base:** Deep charcoal (#0A0A0A) and dark glass surfaces
- **Vibe:** "A media broadcasting operating system." Think premium, cinematic, futuristic, and highly polished.
- **Design Principles:** Glassmorphism, bento-grid layouts, subtle gold glow effects, and high-contrast typography.

### Visual Style Guide
- **Background:** Deep charcoal/black with a very subtle radial glow of `#F5B841` at 5% opacity in the center.
- **Glassmorphism:** Use `backdrop-filter: blur(16px)` on cards and panels with semi-transparent gold borders (`rgba(245, 184, 65, 0.1)`).
- **Typography:** Use a sleek, modern sans-serif font (e.g., Inter, Geist, or Space Grotesk). Headings should be bold and high-contrast.
- **Accents:** Use Studio Gold for primary buttons, active states, status badges, and interactive elements.

## 2. The Existing Architecture (Must Preserve)

This is a vanilla JS/Express app. You must work within this structure.

- **Backend:** Express server running on port 3001.
- **Data:** The board data is stored in a JSON file at `.cursor/boards/msc-website-v9.json`. **The schema must remain untouched.**
- **Integration:** An MCP server connects to Cursor for AI read/write access. Your redesign must not break this.
- **Core Files:** The existing UI is defined by:
  - `/app/index.html`
  - `/app/css/base/_variables.css`
  - `/app/css/main.css`
  - `/app/js/app.js`

## 3. The Redesign Requirements

### UI Layout (Bento + Kanban)
- **Sidebar:** A slim, bento-style sidebar that houses brand logo, quick actions (e.g., "New Campaign"), and service status indicators.
- **Board:** The main Kanban board with columns. Cards should be visually rich, showing assignees, tags, and progress.
- **Status Bar:** A persistent bottom or top bar that displays the real-time status of my AI services:
  - LiteLLM: 🟢 / 🔴
  - ComfyUI: 🟢 / 🔴
  - Postiz: 🟢 / 🔴
  - Hermes: 🟢 / 🔴
- **Agents Activity Feed:** A panel (collapsible) on the side showing recent agent actions.

### Visual Enhancements
- **Cards:** Implement glassmorphism. Cards should have a subtle glow on hover.
- **Columns:** Clearly defined with a gold-accented header. Show a count of cards in each column.
- **Task Detail Modal:** Redesign the modal to feel like a premium overlay with sections for comments, attachments, and a timeline.
- **Animations:** Smooth, subtle animations for dragging cards, opening modals, and status changes.

### Functional & UX Improvements
- **Visual Status for "Blocked":** Cards in the "Blocked" column should have a distinct visual indicator (e.g., a red accent border).
- **Agent Avatars:** Show agent initials (e.g., "M" for Msc, "C" for Coder) on assigned cards.
- **Quick Actions:** From the sidebar, add a button to "Generate Image" which would trigger the `gen-image` command (just a UI button for now).

## 4. Deliverables

Provide me with the complete, updated code for the following files:

1.  `/app/index.html` - The new layout structure.
2.  `/app/css/base/_variables.css` - The CSS variables defining the new color palette and theming.
3.  `/app/css/main.css` - The main stylesheet with all new component styles.
4.  `/app/js/app.js` - The updated JavaScript to handle the new UI elements (service status checks, modals, etc.).

**Do not change the JSON schema.** The board data must continue to work with the existing MCP server.