# Restore Points — Historical Archive

Archived during audit Phase 4 (2026-06-07). **Active checkpoints** (3 most recent) remain in [Restore-Points.md](../Restore-Points.md).

| ID | Date | Summary |
|----|------|---------|
| **RP-2026-06-07-github-releases-v6** | 2026-06-07 | **GitHub releases complete:** Tags `v4.0.0`–`v6.0.0` published; **Latest = `v6.0.0`**. Docs audit PERFECT SYNC. **Branch/commit:** `MSC-Website-v6` @ `17b3da8`. **Restore:** `git fetch --tags origin && git checkout MSC-Website-v6 && git reset --hard 17b3da8 && git pull`. |
| **RP-2026-06-07-v6-start** | 2026-06-07 | **Fresh v6 development branch:** Branch `MSC-Website-v6`, Version `6.0.0`. Cut from `MSC-Website-v5` @ `92918b6`. All operational docs synced to v6. **`MSC-Website-v5`** frozen as backup. **Restore:** `git fetch origin && git checkout MSC-Website-v6 && git reset --hard 17b3da8 && git pull`. |
| **RP-2026-06-07-deploy-hardening** | 2026-06-07 | **Deploy hardening & live DB sync checkpoint:** Fixed Sentry org/project (`mystudiochannel`). Added SSH stop/sync-db scripts, quick DB deploy (`msc:push:db:live`), and `msc:session:stop`. `pushit:live` auto-runs `msc:hostinger:sync-db` after FTPS DB upload. *(Superseded by **RP-2026-06-08-live-v6-sync-app** for full sync-app + npm-install pipeline.)* **`main`** and **`MSC-Website-v5`** pushed @ `06ec2be`. **Restore:** `git fetch origin && git checkout MSC-Website-v5 && git reset --hard 06ec2be && git pull`. |
| **RP-2026-06-07-mcp-awesome-skills** | 2026-06-07 | **MCP Skills and Premium UI Toolkit Checkpoint:** Pre-wired Antigravity Awesome Skills MCP server inside local `.cursor/mcp.json` for agent-ready playbook discovery and execution. Created three structured, portable skill packs under `.cursor/skills/` (DesignMD, GitHub-Ops, and Premium-UI). Integrated Premium UI resources including Cult UI, Aceternity UI, Ali Imam, Animate UI, Its Hover, 21st.dev Magic MCP, and MotionSites.ai. Installed `lenis` smooth scroll and verified Next.js 15 build compatibility. **Branch/commit:** `MSC-Website-v5` @ `1f47162`. **Restore:** `git fetch origin && git checkout MSC-Website-v5 && git reset --hard 1f47162 && git pull`. |
| **RP-2026-06-07-jina-tool-chest** | 2026-06-07 | **Jina Reader and Canonical Tool Chest Checkpoint:** Created master `.cursor/docs/TOOL-CHEST.md` mapping Jina Reader API guidelines, Resend configuration, Stripe API details, and local LiteLLM Google proxy setup. Upgraded `GitHub-Ops` skill pack to educate agents on fast Markdown content fetching. Passed full documentation synchronization checks and pushed branch updates. **Branch/commit:** `MSC-Website-v5` @ `9ec1eaf`. **Restore:** `git fetch origin && git checkout MSC-Website-v5 && git reset --hard 9ec1eaf && git pull`. |
| **RP-2026-06-07-sentry-diagnostics** | 2026-06-07 | **Sentry Next.js 15 & React 19 Active Integration:** Manually installed and configured Sentry for Next.js 15 and React 19 via root-level `instrumentation.ts` bootstrap pipeline and `onRequestError` error hooks. Safeguarded `next.config.mjs` against missing build-time auth tokens. Built active client-side and server-side testing diagnostics card inside `/test/sentry-crash` playground and successfully verified real-time error reporting to Jon's inbox. **Branch/commit:** `MSC-Website-v5` @ `fc8a4cc`. **Restore:** `git fetch origin && git checkout MSC-Website-v5 && git reset --hard fc8a4cc && git pull`. |
| **RP-2026-06-02-v5-hardened** | 2026-06-02 | **Production-Hardening Upgrades Checkpoint:** Implemented local database optimization (`msc:db:optimize` via better-sqlite3), git pre-commit linting guard (Husky), real-time SSH remote log streaming (`msc:logs:live`/`msc:logs:live:console`), deep version tracking inside backup notes, post-backup self-test verification, and backup retention manager (`msc:backup:clean` to retain last 10). Docs synchronized perfectly. **Branch/commit:** `MSC-Website-v5` @ active HEAD. **Git:** Sync to matching SHA. **Restore:** `git fetch origin && git checkout MSC-Website-v5 && git reset --hard HEAD && git pull`. |
| **RP-2026-06-02-v5-start** | 2026-06-02 | **Fresh development branch:** Branch: `MSC-Website-v5`, Version: `5.0.0`. Includes all documentation updates, consolidated master command reference, and automated FTPS WAL/SHM cleanup. **Branch/commit:** `MSC-Website-v5` @ active HEAD. **Git:** Sync of `main` branch to matching SHA. **Restore:** `git fetch origin && git checkout MSC-Website-v5 && git reset --hard HEAD && git pull`. |
| **RP-2026-06-02-v4-master-commands-wal-cleanup** | 2026-06-02 | **Master Command SSoT & WAL Automation Checkpoint:** Created the comprehensive master command index (`MASTER-COMMANDS.md`) cross-linked across the workspace docs and README. Logged 503 missing Hostinger preload folder issue and automated WAL/SHM file deletion with DB size verification in `push-website-live.ps1`. **Branch/commit:** `MSC-Website-v4` @ `0a33e8d`. **Git:** Sync of `main` branch to matching SHA. **Restore:** `git fetch origin && git checkout MSC-Website-v4 && git reset --hard 0a33e8d && git pull`. |
| **RP-2026-06-02-v4-docs-env-template-sync** | 2026-06-02 | **Stable docs + deploy operations checkpoint:** Added deployment troubleshooting playbook, quick decision tree, final configuration audit checklist links, START-HERE verification pointer, and consolidated env templates into single **`.env.example`** (removed duplicate `.env.production.template`). **Branch/commit:** `MSC-Website-v4` @ `35429dc`. **Git:** `main` fast-forward sync planned in closeout. **Restore:** `git fetch origin && git checkout MSC-Website-v4 && git reset --hard 35429dc && git pull`. |
| **RP-2026-06-01-v4-live** | 2026-06-01 | **v4.0.0 live:** Next.js + Payload on **`mystudiochannel.com`** via Hostinger MCP (`MyStudioChannel-v4-deploy.zip`, build **`019e8569-74dc-70e6-bc10-4a121aac92ad`**). Footer **`MyStudioChannel v4.0.0`**. **`/`** + **`/admin`** 200. **Git:** `MSC-Website-v4` / **`main`** synced @ **`87ec9de`**. **Restore:** `git fetch origin && git checkout MSC-Website-v4 && git pull`. **Note:** verify hPanel env vars if API 500. |
| **RP-2026-06-01-v4-start** | 2026-06-01 | Fresh development branch. **Branch:** `MSC-Website-v4`, **Version:** `4.0.0`. Cut from `MSC-Website-v3` @ `8a44d95`. **`MSC-Website-v3`** frozen at v3.0.0. **Restore:** `git fetch origin && git checkout MSC-Website-v4 && git pull`. |
| **RP-2026-06-01-hostinger-live-deploy** | 2026-06-01 | **First live deploy:** Next.js + Payload on **`mystudiochannel.com`** via Hostinger zip upload (WordPress replaced). **Build fix:** `@tailwindcss/postcss`, `postcss`, `tw-animate-css` in **`dependencies`**. **Docs:** `HOSTINGER-DEPLOY.md` (Path A/B/C), `DEPLOYMENT-FIXES.md` (canonical rule). **Git:** `MSC-Website-v3` / **`main`** @ **`c0bdaac`**. **Restore:** `git fetch origin && git checkout MSC-Website-v3 && git pull`. **Ongoing updates:** `npm run pushit:live` → hPanel restart (Path C). |
| **RP-2026-06-01-version-consolidation-main-sync** | 2026-06-01 | **Version + branding:** `package.json` **`3.0.0`** is the only release number; **`lib/msc-app-version.ts`** drives footer (`MyStudioChannel v3.0.0`) and admin sidebar (`MyStudioChannel Admin v3.0.0`). Removed **`lib/msc-admin-version.ts`**. **`Jedi-List`** / **`Go-Live-Checklist`** deploy lists updated. **Git:** `MSC-Website-v3` → **`main`** fast-forward @ **`57910cd`**, both pushed to **`origin`**. **Restore:** `git fetch origin && git checkout MSC-Website-v3 && git pull` (or `git checkout main && git pull`). |
| **RP-2026-05-30-v3-bump** | 2026-05-30 | **Version 3.0.0 Bump:** Successfully merged v2 audit & consolidation into `main`, cut `MSC-Website-v3`, and bumped version to `3.0.0`. Clean bill of health. **Restore:** `git checkout MSC-Website-v3 && git pull`. |
| **RP-2026-05-30-deep-audit-consolidation** | 2026-05-30 | **Deep Audit & Consolidation:** Conducted a comprehensive project audit. Consolidated legacy references, purged project bleed (removed Vader-Engine & cPanel/Spaceship remnants), created `TRUTH.md` as the master single source of truth, created `jon-operator-hpanel.mdc` for Hostinger hPanel. Verified clean build using `verify:next:safe` and verified 100% success on local endpoints via `msc:verify:local`. **Restore:** `git checkout MSC-Website-v3 && git pull`. |
| **RP-2026-05-30-hostinger-mcp-audit** | 2026-05-30 | **Hostinger Connectivity & Audit:** Established working Hostinger MCP connection and completed a full pre-deployment audit. Created `HOSTINGER-DEPLOY.md` and env template docs baseline (now consolidated into `.env.example`). **Restore:** `git checkout main && git pull`. |
| **RP-2026-05-30-unified-workflow-setup** | 2026-05-30 | **Unified Workflow System:** Created robust prompt runners (`Start-Project.md`, `End-Project.md`, `Update-Project.md`, `Update-Docs.md`) and tracking logs (`project-log.md`, `Checkpoint.md`) coordinated by `workflow.mdc` under `MSC-Website-v3`. Clean, custom, and anonymous (no Vader-Engine refs). **Restore:** `git fetch origin && git checkout MSC-Website-v3 && git pull`. |
| **RP-2026-05-30-initial-release-v100** | 2026-05-30 | **Initial GitHub release:** Created public repo `jonbeatz/MyStudioChannel` and tagged/released `v1.0.0` on `main`. **Restore:** `git fetch origin && git checkout v1.0.0`. |
| **RP-2026-05-30-workspace-sync-rename** | 2026-05-30 | **Workspace sync complete:** Updated all hardcoded paths to `D:\Cursor_Projectz\MyStudioChannel` across docs and scripts after project directory rename. **Status:** Verified health via `npm run msc:verify:local`. |
| **RP-2026-05-29-litellm-gemini-3.5-flash** | 2026-05-29 | **LiteLLM Google API Proxy + ngrok fully verified:** Vertex AI Gemini 3.5 Flash (`vader-3.5-flash`) and `google-api/ngrok.exe` are fully set up. LiteLLM runs on port **4000** and ngrok provides secure public HTTPS tunneling on port **4040** (inspector). **Restore:** Run `npm run msc:google-api:start` (starts both) and check connectivity using `npm run msc:litellm:test:ngrok`. |
| **RP-2026-04-08-cms-globals** | 2026-04-08 | **Payload “Site” content model:** `globals/Homepage.ts` (hero slides + stats from **Media**), `globals/SiteSettings.ts` (SEO title/tagline → `generateMetadata`). Collections: **Leads** (`collections/Leads.ts`, Marketing group, public `create` for future forms). Marketing **Hero** reads CMS when slides exist; else built-in defaults. Lib: `lib/cms/homepage.ts`, `site-settings.ts`, `content-types.ts`. **Docs** updated: Development, ReCall, Site-Plans. |
| **RP-2026-04-08-hero-media-relinked** | 2026-04-08 | **Hero migration finalized:** Homepage global now contains **5 slides** (the original 4 defaults + current custom slide). The 4 original slides were mapped to real `Media` entries sourced from on-disk assets (then under `public/images`, now consolidated to **`public/media`**) (`tv-wall.jpg`, `show-cards.jpg`, `on-air.jpg`, `creator-solo.jpg`) via a one-time local migration script, then script removed. |
| **RP-2026-04-08-working-resend-email** | 2026-04-08 | **Resend verification flow fully working:** Added browser-safe verify endpoint in `collections/Leads.ts` (`/api/leads/verify/:token`, with `?token=` fallback), confirmation redirect to `/?verified=success|error`, and homepage verification toast with NovaMira gold styling + mobile top-center placement in `components/contact-section.tsx`. Removed direct-booking helper text from contact card and centered newsletter success confirmation. Synced docs in Development + ReCall. |
| **RP-2026-04-08-seo-setup** | 2026-04-08 | **SEO setup completed:** official `@payloadcms/plugin-seo` installed/configured; SEO tab enabled for `pages` + `hero-slides`; `Homepage.heroSlides` rows now include inline `seo` group fields; homepage `generateMetadata` follows active slide SEO title/description/image with fallbacks to slide content and Site Settings; OG/Twitter metadata now mirrors active slide SEO. |
| **RP-2026-04-08-two-step-booking-engine** | 2026-04-08 | **Professional two-step booking flow live:** `bookings` schema updated to `name/email/phone/appointmentDate/message`; contact modal refactored to Step 1 (time select) then Step 2 (details), with Payload API save + gold success toast. `bookings` afterChange hook now sends branded confirmation email to user and admin alert via Resend on create. |
| **RP-2026-04-08-command-center-integration** | 2026-04-08 | **Centralized Command Center integrated:** Added `Site settings -> Notifications` tab (`enableAdminNotifications`, `notificationEmails`, `adminFallbackEmail`, `systemFromEmail`) and refactored `bookings` + `leads` notification hooks to read all recipients/toggles/from-address from global settings with resilient, isolated email send error handling. Booking/newsletter UI now uses unified gold tokens and confirms success toast only after complete booking submission. |
| **RP-2026-04-08-command-center-polish-and-email-branding** | 2026-04-08 | **Final polish + reliability pass:** fixed Site Settings admin 404 caused by local SQLite schema drift (`site_settings_notification_emails` table + Site Settings notification columns), added live “Effective Recipient List” admin preview + Sender Address labeling, grouped `bookings` and `leads` under Marketing, and updated Leads/Bookings email templates to enforce gold no-underline links while streamlining New Lead Alert to email-only content. |
| **RP-2026-04-08-admin-ux-navigation-consolidation** | 2026-04-08 | **Admin authoring UX standardized:** Projects moved to `projects-home` global with draggable row editing (Homepage-style), header submenus restored via nested `header.navItems.submenu`, demos rail interaction/scroll polish completed, and admin sidebar/account navigation refined (gold category labels, Dashboard link, account-context Log out behavior). |
| **RP-2026-04-08-dynamic-pages-sections-builder** | 2026-04-08 | **Dynamic pages elevated:** added `Site settings -> Branding` tab and frontend logo/metadata wiring, created dynamic slug engine (`app/(site)/[slug]/page.tsx`), upgraded `pages` with `featuredImage`, Lexical `content`, and `sections` blocks (rich text, feature grid, video player) with required anchor IDs, plus sticky in-page jump links (mobile chip scroller + active auto-centering). Includes local SQLite schema patching for `pages` columns and `pages_blocks_*` tables. |
| **RP-2026-04-08-pages-polish-and-demo-flow** | 2026-04-08 | **Dynamic pages final polish checkpoint:** made jump-link nav non-sticky by design, hid fallback content panel when sections are present, tightened section spacing, and seeded full `msc1` demo storyline for anchor-flow testing (`The Studio` → `Virtual Tour` → `Our Solutions` → `Case Study` → `Get Started`). |
| **RP-2026-04-09-spaceship-live-recovery** | 2026-04-09 | **Production recovery on Spaceship shared hosting:** pinned runtime compatibility (`next@15.4.11`, Payload 3.81 exact), moved `patch-package` into dependencies for production postinstall, updated `server.js` host binding to `0.0.0.0`, introduced deploy scripts `msc:pushitup` + `msc:pushitupzip`, documented low-memory host workflow (local build + upload prebuilt `.next` zip), uploaded `patches/` to restore Payload admin behavior, and confirmed public site + `/admin/login` live. |
| **RP-2026-04-09-email-verify-redirect-final** | 2026-04-09 | **Verification redirect finalized on live host:** `collections/Leads.ts` verify endpoint now uses relative redirect targets (`/?verified=success|error`) to avoid reverse-proxy/internal host leakage (`0.0.0.0:3000`). Email links use `NEXT_PUBLIC_SERVER_URL` for public origin. Confirmed live homepage toast appears after verification. |
| **RP-2026-04-10-live-vendor-chunks-recovery** | 2026-04-10 | **Live + local stability restored after chunk mismatch incident:** live `500` traced to mixed server `.next` artifacts (`Cannot find module './vendor-chunks/@payloadcms.js'`, `date-fns.js`, `next.js`). Recovery used nodevenv activation, server `.next` reset, local rebuild, full `.next` upload from PC, targeted chunk re-upload on FTPS failures, and Node app restart. Local port drift issue (3000 hijacked) resolved via stale process kill + `npm run dev:fresh`; `msc:verify:local` returns all `200`. |
| **RP-2026-04-10-toolchain-docs-operator** | 2026-04-10 | **Demos section fixes + ESLint (`next lint`) + admin **v1.0.4** confirmed live.** Docs: `Jedi-List` lint row synced to `package.json`; `Spaceship` points to `START-HERE` / `jon-operator-cpanel` for Jon’s cPanel bookmarks; `ReCall` session log updated. **Restore:** commit on branch `mscNowLive-v3-RestorePoint`; local `npm install` if lockfile restored from earlier SHA. |
| **RP-2026-04-11-snapshot-next-payload-media** | 2026-04-11 | **Snapshot (`Take a snapshot`):** unified static assets under **`public/media`** with **`/media/...`** URLs; **`msc:media:consolidate`** + **`msc:media:sync`**; dev scripts **`clean:next`** before **`dev`**; Cursor **`stop`** hook **`verify-after-agent`**; **`verify:next`** green before commit. Docs SSoT updates (**Jedi-List**, **Go-Live-Checklist**, **Site-Plans**, **Headless-WP-Backend-Plan**, **ToDo**, **Custom-Prompts**). **Ongoing work:** branch **`mscNowLive-v4-RestorePoint`** (same commit as **`snapshot/2026-04-11-msc-next-payload-media`**). **Restore:** `git fetch origin && git checkout mscNowLive-v4-RestorePoint`. **Caveats:** back up **`payload.sqlite`** if you need matching CMS data; run **`npm install`** after checkout if lockfile differs. |
| **RP-2026-04-11-msc-pro-admin-branding** | 2026-04-11 | **Snapshot (`Take a snapshot`):** **MSC PRO ENGINE** Payload admin — custom **`graphics`** Logo/Icon (`/media/msc-icon.png`), login password toggle (**`msc-payload-admin-enhancements`**), **`payload.config.ts`** providers + graphics, **`collections/Users.ts`** notes, **`app/(payload)/custom.scss`**, **`MSC_ADMIN_VERSION` 1.0.5**, regenerated **`importMap.js`**. **`package.json`:** **`msc:pushitup:admin-ui`** (full admin bundle) + **`msc:pushitup:admin-branding`** (subset). Docs synced (**Jedi-List**, **Spaceship**, **Go-Live-Checklist**, **Custom-Prompts** §37, **ReCall** deploy-sync). **`verify-next-safe.ps1`**, Cursor hook **`guard-clean-while-dev`**. **`npm run build`** green before commit. **Tip:** `mscNowLive-v4-RestorePoint` @ **`6b84052`**. **Parity snapshot branch:** **`snapshot/2026-04-11-msc-pro-admin-branding`** (same commit). **Restore:** `git fetch origin && git checkout mscNowLive-v4-RestorePoint && git pull` (or `git checkout snapshot/2026-04-11-msc-pro-admin-branding && git pull`). **Caveats:** back up **`payload.sqlite`** if needed; **`npm install`** if lockfile differs. |
| **RP-2026-04-12-snapshot-ftps-parity** | 2026-04-12 | **Snapshot (`Take a snapshot`):** **Tier-2 deploy + parity tooling.** **`scripts/pushit-live.ps1`** — 6-step pipeline (build → admin-ui → `.next` → **`payload.sqlite`** → **`public/media`** → **`dev:fresh`**), fixed PowerShell quoting for sqlite reminder, **`cd /home/wjehbnzcoy/mystudiochannel.com`**. **`scripts/PushItUP.ps1`** — honor **`remotePath`** when FTPS LIST fails (chroot); **`FileShare.ReadWrite`** read for **`payload.sqlite`** upload while DB open; upload error logging. **`scripts/ftp-parity-check.ps1`** + **`parity-ftp-report.md`** — local vs live tree compare (last run: **`payload.sqlite`** 536576 bytes match; **`public/media`** 33 files both sides; **`.next`** differs when local is dev vs prod). **Branch/commit:** **`mscNowLive-v4-RestorePoint`** @ **`a8c72f9`**. **Restore (committed tree only):** `git fetch origin && git checkout mscNowLive-v4-RestorePoint && git reset --hard a8c72f9ee4d5793be3a66332d0c0d0520e52648e`. **Caveats:** working copy had **uncommitted + untracked** files at snapshot time — stash or commit WIP before `reset --hard`; back up **`payload.sqlite`**; optional tag: `git tag RP-2026-04-12-snapshot-ftps-parity a8c72f9ee4d5793be3a66332d0c0d0520e52648e`. |
| **RP-2026-04-12-docs-checkpoint** | 2026-04-12 | **Docs checkpoint (`Lets Checkpoint Docs`):** **`package.json`** **`msc:parity:ftp`** alias; **Jedi-List**, **Custom-Prompts**, **Go-Live-Checklist**, **Spaceship**, **START-HERE**, **Agent-Runbook**, **ReCall** synced to **Tier 2** (`pushit:live` includes **`payload.sqlite`** + **`public/media`**; cPanel **`cd /home/wjehbnzcoy/mystudiochannel.com`**). **PushItUP** LIST/`remotePath` note in **Jedi-List**. No git step required for this checkpoint. |
| **RP-2026-04-12-branch-tip-ready-tomorrow** | 2026-04-12 | **Resume here after a cold start:** branch **`MSC-Website-v3`** (pull latest — doc sync landed 2026-04-12). **Code line:** **`38dd8f8`** — homepage hero from **`globals/Homepage`** + **Media** ( **`collections/HeroSlides.ts` removed** ); **`lib/email-brand.ts`**, **`lib/email-templates.ts`**; **`payload-types.ts`** + section/CMS updates. **Ops/docs:** **`fbb6afd`** ReCall closeout; **`b92d1be`** **`parity-ftp-report.md`** gitignored; evening pass synced **Restore-Points**, **ReCall** Current focus, **Ready to begin** (**START-HERE** / **Agent-Runbook §0** / **Custom-Prompts** item **0** / **Jedi-List** `msc:parity:ftp` note). **Restore:** `git fetch origin && git checkout MSC-Website-v3 && git pull`. **Tomorrow:** paste **`Ready to begin`** from **Agent-Runbook §0** (or **Custom-Prompts** item **0**); then **`npm run dev:fresh`** if **`/`** or **`/admin`** are not healthy. |
| **RP-2026-04-13-live-ftps-db-recovery** | 2026-04-13 | **Snapshot (`Take a snapshot`):** **Live** **`https://mystudiochannel.com`** homepage + **`/admin`** with **`v1.0.6`** after **Tier 2** deploy (**`pushit:live`**) with FTPS **`Remote base: /`** (fix wrong trees: **`/home/...`** and **`/mystudiochannel.com/`** double folder). **`payload.sqlite`** recovery: stop Node, **`rm -f payload.sqlite-wal payload.sqlite-shm`**, **`npm run pushitup -- payload.sqlite`**, cPanel **`sqlite3 ./payload.sqlite "UPDATE media SET url = '/media/' || filename;"`**, restart Node — resolves **`malformed database schema (bookings_…_idx)`** when WAL/orphan indexes bite. **Docs/scripts:** **Spaceship.md** § FTP + § SQLite **4)**; **Jedi-List** **`msc:pushitup:ftp-smoke`**, **`msc:verify:ftp-smoke`**, **`msc:migrate:sqlite:fix-bookings-table`**; **`scripts/fix-sqlite-bookings-table.py`** + **`.sql`**, **`verify-ftp-smoke-remote.ps1`**, **`ftp-path-smoke-test.txt`**; **PushItUP** / **pushit-live** / **Test-SpaceshipFtp** comments; **START-HERE**, **Go-Live-Checklist**, **Agent-Runbook**, **Custom-Prompts** path notes. **Routes:** static **`/privacy-policy`**, **`/terms-of-service`**, **`middleware.ts`** reserved segments. **Local:** **`msc:verify:local`** **PASS** (`/`, `/admin`, projects-home API) with dev on **3000**. **Branch/commit (pushed tip at snapshot):** **`mscNowLive-v4-RestorePoint`** @ **`6c9e3cc14d2c516617f8082b27a2e46b445e8b10`**. **Restore (committed tree only):** `git fetch origin && git checkout mscNowLive-v4-RestorePoint && git reset --hard 6c9e3cc14d2c516617f8082b27a2e46b445e8b10`. **Caveats:** working copy had **many modified + untracked** files at snapshot — **`git stash push -u -m "pre-RP-2026-04-13"`** or **commit** before **`reset --hard`**; back up **`payload.sqlite`**; keep **`.vscode/sftp.json`** **`remotePath`** **`/`**; optional tag: `git tag RP-2026-04-13-live-ftps-db-recovery 6c9e3cc14d2c516617f8082b27a2e46b445e8b10`. |
| **RP-2026-04-13-checkpoint-v107-env-nav-leads** | 2026-04-13 | **Checkpoint (`Lets Checkpoint Docs + Commit`):** **`v1.0.7`** (**`lib/msc-admin-version.ts`** + **`package.json`**). **Public URL protocol:** **`lib/site-origin-defaults.ts`**, **`lib/public-origin.ts`** (`getPublicOrigin`, `getPublicOriginClient`, `buildPayloadCsrfOriginList`), **`payload.config.ts`**, **`next.config.mjs`** — production fallback **`https://mystudiochannel.com`** (override **`MSC_CANONICAL_SITE_ORIGIN`**); **`.env.example`** + docs (**Jedi-List**, **START-HERE**, **Spaceship**). **Admin:** fix React **hydration** on **View site** / **View Page** (client-only origin). **Leads:** **`preflightDeleteAuthUserRows`** **`beforeDelete`** for SQLite **`payload-preferences`**. **Marketing nav:** measured header offset, mobile **`deferMs`** scroll after drawer close; footer hash **`onHashAnchorClick`**; **`HomeHashScroll`** + **`globals.css`** **`scroll-margin`**. **Routes / UI:** **`/privacy-policy`**, **`/terms-of-service`**, **`app/(site)/pages/`**, contact modal stack, assorted sections. **Ops:** **`msc:migrate:sqlite:fix-bookings-table`**, FTPS smoke scripts. **Branch/commit:** **`mscNowLive-v4-RestorePoint`** @ **`41ab969`**. **Restore:** `git fetch origin && git checkout mscNowLive-v4-RestorePoint && git pull` (or `git reset --hard 41ab969` for exact tree). **Caveats:** local **`.env.local`** needs **`NEXT_PUBLIC_SERVER_URL`** for admin CSRF; live host: set **`NEXT_PUBLIC_*`** / **`PAYLOAD_PUBLIC_*`** per **Spaceship.md**; deploy **`pushit:live`** to refresh live **v1.0.7** + URL behavior. |
| **RP-2026-04-13-live-deploy-hardening** | 2026-04-13 | **Snapshot (`Take a snapshot`):** Live site confirmed healthy at `https://mystudiochannel.com` — Demos section shows **Talk Show Land** as featured with correct images. Full deploy emergency resolved (session started with live 500). Three root causes identified and fixed: (1) **Stale `.next` on server** — FTP merges not replaces; old `webpack-runtime.js` referenced non-existent `vendor-chunks` → 500. Fix: `rm -rf .next` before every Tier 2 upload. (2) **SQLite WAL replay** — `payload.sqlite-wal` / `payload.sqlite-shm` caused SQLite to replay old journal on freshly-uploaded DB → stale project data returned. Fix: `rm -f payload.sqlite-wal payload.sqlite-shm` before every DB upload. (3) **Missing `media` table rows** — `projects_home_project_items` referenced image IDs 33–37 missing from server `media` table → `mapProjectItemsToDemos` filtered all rows → `FALLBACK_DEMOS` rendered. Fix: `INSERT OR IGNORE` via cPanel `sqlite3` + `UPDATE media SET url = '/media/' || filename`. **Docs hardened:** `Spaceship.md` new pre-upload cleanup section + §4a media table sync troubleshooting; `Custom-Prompts.md` items 3 + 38 with pre-upload block; `scripts/pushit-live.ps1` runtime `Write-Host` warnings; `ReCall.md` session entry. **Branch/commit:** `mcs-Live-v5-Restore` — commit SHA captured after this entry is committed (see next SHA in log). **Restore:** `git fetch origin && git checkout mcs-Live-v5-Restore && git pull`. **Caveats:** back up `payload.sqlite` if CMS data matters; `npm install` after checkout if lockfile differs; run pre-upload cleanup steps before every future `pushit:live`. |
| **RP-2026-04-14-live-admin-homepage-parity-restored** | 2026-04-14 | **Snapshot (`Take a snapshot`):** live parity restored after repeated drift between local and host. Symptoms were **`/admin/msc-homepage` 404** and stale demos on live while local was correct. **Confirmed drift:** local `payload.sqlite` ~540 KB with `homepage.is_styles_visible`; live file was tiny/old (~57 KB). **Also caught path/layout risk:** nested/partial `.next` during recovery can leave runtime inconsistent. **Working recovery sequence:** Stop app → `rm -rf .next` + `rm -f payload.sqlite-wal payload.sqlite-shm`; Local (PC) `npm run build` then `npm run pushitup -- .next`, `npm run pushitup -- payload.sqlite`, `npm run pushitup -- public/media`; Live terminal `sqlite3 ./payload.sqlite "UPDATE media SET url = '/media/' || filename;"` + `pkill -u $(whoami) node`; Start app. **Result:** live homepage now matches local (Talk Show Land featured) and admin homepage route loads. **Session commits:** `97f4421` (Projects admin polish), `327d843` + `68e1f3e` (rowLabel serialization/type fixes). **Branch/commit at closeout:** `feature/projects-admin-polish` @ `68e1f3e` (plus docs in this checkpoint). **Restore:** `git fetch origin && git checkout feature/projects-admin-polish && git pull`. **Caveats:** never trust deploy if live `payload.sqlite` size is far smaller than local; avoid nested `.next` trees; keep pre-upload cleanup mandatory each Tier 2 run. |
| **RP-2026-04-13-homepage-galleries-drawer-hydrate** | 2026-04-13 | **Docs checkpoint (`Lets Checkpoint Docs`):** **Programming Styles** + **Services Gallery** on **`globals/Homepage`**: each slot’s **`image`** stays **`relationship` → `media`**, **`required: false`** (clear slot → static fallback). **Admin:** Payload 3 **`admin.appearance: 'drawer'`** + **`allowCreate` / `allowEdit`** (not `admin.upload` on relationship — use **[Relationship field docs](https://payloadcms.com/docs/fields/relationship)**). **Hydration:** **`lib/cms/homepage-gallery-hydrate.ts`** + **`lib/cms/homepage-gallery-seed.ts`** — **`afterRead`** pads 7 rows, respects **`image: null`**, normalizes IDs/partial docs to a **complete Media shape** (`id`, **`filename`**, **`url`**) so the rich picker stays usable. **Site:** **`components/services-section.tsx`**, **`app/(site)/page.tsx`**, **`lib/cms/homepage.ts`**, **`lib/cms/content-types.ts`**, **`payload-types.ts`**. **Version:** **`v1.0.8`**. **Build/ops confirmed:** **`npm run msc:generate:types`**, **`npm run verify:next:safe`** green; **`npm run dev:recover`** + **`npm run msc:verify:local`** PASS (`/`, `/admin`, projects-home API). **Branch @ doc time:** **`mcs-Live-v5-Restore`** @ **`dce1299`** — **working tree was dirty** (modified + **untracked** gallery files). **Capture this state:** `git add -A && git commit -m "Homepage galleries: drawer relationship + seed hydrate"` (or stash `-u`). **Restore last commit only:** `git fetch origin && git checkout mcs-Live-v5-Restore && git reset --hard dce1299` (**drops** uncommitted work — commit/stash first). **Caveats:** back up **`payload.sqlite`** if CMS rows matter; after checkout run **`npm install`** if lockfile differs; optional tag after commit: `git tag RP-2026-04-13-homepage-galleries-drawer-hydrate <sha>`. |
