---
description: Global chat shortcuts — backup ritual (interactive + quick)
alwaysApply: true
---

# Global shortcuts

## Backup ritual

| Operator says | Agent action |
|---------------|--------------|
| **`backup project`**, **`create backup`**, **`backup`** | Interactive ritual — **AskQuestion** buttons each step ([CURSOR.md](../custom-scriptz/backup-system/CURSOR.md), [interactive-workflows.mdc](./interactive-workflows.mdc)) |
| **`backup quick`**, **`backup project quick`** | **Non-interactive:** run `npm run msc:backup:quick` from repo root — no questions, no note prompt |
| **`backup full`**, **`backup project full`** | **Non-interactive:** run `npm run msc:backup:quick:full` from repo root — no questions, no note prompt |
| **`clean backups`**, **`purge old backups`** | Run `npm run backup:clean` (deletes backups older than the 10 most recent) |

Quick commands use default backup root (`G:\Cursor_Project_BackUpz\MyStudioChannel`), auto-suggest next `msc-website-v1-*` folder, and `--yes`.

After quick backup completes, report destination path and `.cursor/BackUp-Notez.md`.

## Push website live

| Operator says | Agent action |
|---------------|--------------|
| **`push website live`**, **`push it live`**, **`push site live`**, **`deploy live`** | Run **Push Website Live** ([`.cursor/prompts/Push-Website-Live.md`](../prompts/Push-Website-Live.md)) — **`AskQuestion` first:** Quick DB · **Fast FTPS (`pushit:live:fast`)** · **Full FTPS** · MCP (avoid on this host). Do **not** auto-run full build without Jon’s choice. |

**Local commands:** `npm run msc:push:db:live` (Quick DB) · `npm run push:website:live` (MCP code-only) · `powershell -File scripts/push-website-live.ps1 -Ftps` (full) · dry-run: `-- --dry-run`

**Deployment zips:** `zips/MyStudioChannel-deploy-YYYYMMDD-HHmmss.zip` only (never repo parent folder).

**Restart link (always after success):** https://hpanel.hostinger.com/websites/mystudiochannel.com

## Database copy

| Operator says | Agent action |
|---------------|--------------|
| **`copy database`**, **`db copy`**, **`safe copy db`** | Run **`npm run db:copy`** from repo root, then explain upload/rename/WAL cleanup/restart steps. |

## Gilfoyle Usage Restriction

The Gilfoyle prompt ([`harsh-review.md`](../prompts/harsh-review.md)) is for **code review only**.

**Never use Gilfoyle mode for:**

- Deployment commands (`pushit:live`, `pushit:live:fast`, `push:website:live`, `msc:push:website:live`)
- Documentation sync (`update docs`, `msc:docs:sync`, `sync`)
- Hostinger operations (`msc:hostinger:*`, SSH sync scripts)
- Production database changes (`db:copy` upload, `msc:push:db:live`)

Use Gilfoyle only when the operator explicitly requests a harsh code or UI critique pass.
